init - npm init -y
nvm - github code
dependencies - npm i express bcrypt jsonwebtoken

in package.lock
    // watch automatically restarts the server
    // --env-file=.env  loads var from env
    // --strip-types removers types so doesn't crash
    // --experimental-sqlite


for using this ==> import express from 'express'
we have to do this ==> "type":"module" in package.lock


BOILERPLATE
import express from 'express';

const app = express()
// IF THERE IS ENV PORT IN ENV THEN USE THAT ELSE 5000
const PORT = process.env.PORT || 5000



app.listen(PORT,()=>{
    console.log(`server has started on port: ${PORT}`);
    
})